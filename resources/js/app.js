import './bootstrap';
import { Tooltip, 
        Collapse, 
        Dropdown, 
        Input,
        Modal,
        Ripple,
        Carousel,
        Autocomplete,
        Select,
        initTE } from "tw-elements";
initTE({ Tooltip, Collapse, Dropdown, Input, Ripple, Modal, Carousel, Autocomplete, Select });

